name: "Helllo World Workflow"

on:
    push:
        branches:
            - master
            - production 
             
jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: "Print Hello World"
              run: echo "Hello World"
            - name: "Print date"
              run: date